id: space.rirusha.Cassette.Devel
runtime: org.gnome.Platform
runtime-version: 'master'
sdk: org.gnome.Sdk
command: cassette
finish-args:
  - --share=network
  - --share=ipc
  - --socket=fallback-x11
  - --device=dri
  - --socket=pulseaudio
  - --socket=wayland
  - --filesystem=xdg-music:ro
  - --filesystem=xdg-download:ro
  - --own-name=org.mpris.MediaPlayer2.space.rirusha.Cassette.Devel
  - --env=SERIALIZE_UNKNOWN_PROPS=1
  - --env=SERIALIZE_UNKNOWN_FIELDS=1
modules:
  - name: libapi-base
    buildsystem: meson
    builddir: true
    sources:
      - type: git
        url: https://altlinux.space/rirusha/libapi-base.git
        tag: v7.10
  - name: libcase
    buildsystem: meson
    builddir: true
    sources:
      - type: git
        url: https://altlinux.space/rirusha/libcase.git
        tag: v0.2
  - name: cassette
    buildsystem: meson
    builddir: true
    config-opts:
      - -Dnightly=true
    sources:
      - type: dir
        path: ../../
