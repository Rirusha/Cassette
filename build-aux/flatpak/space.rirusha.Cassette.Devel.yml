id: space.rirusha.Cassette.Devel
runtime: org.gnome.Platform
runtime-version: master
sdk: org.gnome.Sdk
command: cassette
finish-args:
  - --share=network
  - --share=ipc
  - --socket=fallback-x11
  - --device=dri
  - --socket=pulseaudio
  - --socket=wayland
  - --filesystem=xdg-music:ro
  - --filesystem=xdg-download:ro
  - --own-name=org.mpris.MediaPlayer2.space.rirusha.Cassette.Devel
  - --env=SERIALIZE_UNKNOWN_PROPS=1
  - --env=SERIALIZE_UNKNOWN_FIELDS=1
build-options:
  append-path: /usr/lib/sdk/vala/bin
  prepend-ld-library-path: /usr/lib/sdk/vala/lib
cleanup:
  - /include
  - /lib/pkgconfig
  - /share/pkgconfig
  - /share/vala
  - "*.a"
modules:
  - name: api-base
    buildsystem: meson
    builddir: true
    sources:
      - type: git
        url: https://altlinux.space/rirusha/libapi-base.git
        branch: main
  - name: libtape
    buildsystem: meson
    builddir: true
    sources:
      - type: git
        url: https://gitlab.gnome.org/Rirusha/libtape.git
        branch: main
  - name: cassette
    buildsystem: meson
    builddir: true
    config-opts:
      - -Dis_devel=true
    sources:
      - type: dir
        path: ../../
