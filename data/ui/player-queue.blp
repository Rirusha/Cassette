using Gtk 4.0;
using Adw 1;

template $CassettePlayerQueue : $CassetteSidebarChildBin {
  title: _("Playing now");

  ScrolledWindow scrolled_window {
    hscrollbar-policy: never;
    propagate-natural-height: true;
  }
}