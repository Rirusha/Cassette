using Gtk 4.0;
using Adw 1;

template $CassetteContent: Adw.Bin {
  ScrolledWindow {
    propagate-natural-height: true;
    hscrollbar-policy: never;
    overflow: hidden;

    $CassetteListView list_view_wrap {
      spacing: 12;
    }
  }
}
